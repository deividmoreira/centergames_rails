<h1>Listing valor_fichas</h1>

<table>
  <tr>
    <th>Descricao</th>
    <th>Preco</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @valor_fichas.each do |valor_ficha| %>
  <tr>
    <td><%= valor_ficha.descricao %></td>
    <td><%= valor_ficha.preco %></td>
    <td><%= link_to 'Show', valor_ficha %></td>
    <td><%= link_to 'Edit', edit_valor_ficha_path(valor_ficha) %></td>
    <td><%= link_to 'Destroy', valor_ficha, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Valor ficha', new_valor_ficha_path %>
