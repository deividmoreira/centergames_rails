<h2>Cargos</h2>
<%= link_to 'Novo', new_cargo_path, :class => 'novo' %>
<table>
	<thead>
	  <tr>
	    <th>Nome</th>
	    <th></th>
	  </tr>
	</thead>
	<tfoot></tfoot>
	<tbody>
		<% @cargos.each do |cargo| %>
		  <tr>
		    <td><%= cargo.nome %></td>
		    <td>
		    	<%= link_to 'Mostrar', cargo, :class => 'mostrar' %>
		    	<%= link_to 'Editar', edit_cargo_path(cargo), :class => 'editar' %>
		    	<%= link_to 'Excluir', cargo, confirm: 'Confirma exclusão?', method: :delete, :class => 'excluir' %>
		    </td>
		  </tr>
		<% end %>
	</tbody>	
</table>